<%= form_for(@post) do |f| %>
	
	<%= f.label :title %>
	<%= f.text_field :title %><br>

	<%= f.label :content %>
	<%= f.text_area :content :class => 'editing' %><br>

	<%= f.label :image %>
	<%= f.file_field :image %>

	<%= f.submit :class => 'btn btn-primary' %>

<% end %>